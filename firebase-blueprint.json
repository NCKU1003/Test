{
  "entities": {
    "shared_photos": {
      "title": "SharedPhoto",
      "description": "Uploaded travel photos shared with everyone in the cloud",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique document ID (e.g. spotId_timestamp)"
        },
        "spotId": {
          "type": "string",
          "description": "The tourist spot ID this photo belongs to"
        },
        "photoData": {
          "type": "string",
          "description": "The base64-encoded, compressed image data"
        },
        "uploadedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the photo was uploaded"
        }
      },
      "required": ["id", "spotId", "photoData", "uploadedAt"]
    },
    "wishlist": {
      "title": "WishItem",
      "description": "Places suggested by users that they wish to visit",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Document ID"
        },
        "name": {
          "type": "string",
          "description": "Name of the requested spot"
        },
        "notes": {
          "type": "string",
          "description": "Notes on why they want to visit"
        },
        "category": {
          "type": "string",
          "description": "Category of the spot"
        },
        "author": {
          "type": "string",
          "description": "Name of the person making the wish"
        },
        "likes": {
          "type": "integer",
          "description": "Total upvotes/likes"
        },
        "likedBy": {
          "type": "array",
          "description": "List of user UIDs who upvoted"
        },
        "gmapUrl": {
          "type": "string",
          "description": "Google Maps query URL"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of creation"
        }
      },
      "required": ["id", "name", "author", "likes", "likedBy", "createdAt"]
    },
    "custom_spots": {
      "title": "CustomSpot",
      "description": "Spots added from the wishlist to the shared itinerary",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique spot document ID"
        },
        "wishId": {
          "type": "string",
          "description": "Optional reference to the original wish item"
        },
        "dayNumber": {
          "type": "integer",
          "description": "The day number (1-4) this spot is scheduled on"
        },
        "time": {
          "type": "string",
          "description": "Scheduled time (e.g. 14:00)"
        },
        "name": {
          "type": "string",
          "description": "Name of the custom spot"
        },
        "description": {
          "type": "string",
          "description": "Description or notes of the custom spot"
        },
        "type": {
          "type": "string",
          "description": "The category type of the spot"
        },
        "gmapUrl": {
          "type": "string",
          "description": "Optional Google Maps search/location URL"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "When this spot was added to the itinerary"
        }
      },
      "required": ["id", "dayNumber", "time", "name", "type", "createdAt"]
    }
  },
  "firestore": {
    "/shared_photos/{photoId}": {
      "schema": "shared_photos",
      "description": "Shared travel memories collection, keyed by spotId"
    },
    "/wishlist/{wishId}": {
      "schema": "wishlist",
      "description": "A collection storing travel wishes, accessible by everyone"
    },
    "/custom_spots/{spotId}": {
      "schema": "custom_spots",
      "description": "Custom spots added to the itinerary from the wishlist"
    }
  }
}
